name: CI

# Full-stack CI covering core + CLI + Web + Playwright (report P1-06 / §13.13).
# core.yml remains the focused core gate; this workflow adds the layers that
# most affect human usability and were previously unprotected on the main
# branch: root typecheck/lint, CLI build/unit/e2e, Web build/typecheck/unit,
# and the browser (Playwright) journeys.

on:
  push:
    branches:
      - main
      - rebuild-v2
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'
  # pnpm 版本由根 package.json 的 packageManager 字段钉死，corepack 按此解析。
  # 注意：corepack 随 Node 分发的区间是 >=14.19.0 <25.0.0，升级 Node 25 前需改用显式 setup。

jobs:
  typecheck:
    name: Root typecheck + lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check installer syntax
        run: bash -n scripts/install.sh

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Build all packages
        run: corepack pnpm -r build

      - name: Typecheck all packages
        run: corepack pnpm -r typecheck

  cli:
    name: CLI build + unit + e2e
    runs-on: ubuntu-latest
    needs: typecheck
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Build core + CLI
        run: |
          corepack pnpm --filter @tekon/core build
          corepack pnpm --filter @tekon/cli build

      - name: CLI unit tests
        run: corepack pnpm --filter @tekon/cli test:unit

      - name: CLI e2e tests
        run: corepack pnpm --filter @tekon/cli test:e2e

  web:
    name: Web build + typecheck + unit
    runs-on: ubuntu-latest
    needs: typecheck
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Build core (web depends on it)
        run: corepack pnpm --filter @tekon/core build

      - name: Web typecheck
        run: corepack pnpm --filter @tekon/web typecheck

      - name: Web build
        run: corepack pnpm --filter @tekon/web build

      - name: Web unit tests
        run: corepack pnpm --filter @tekon/web test

  web-e2e:
    name: Web Playwright e2e
    runs-on: ubuntu-latest
    needs: web
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Build core (web depends on it)
        run: corepack pnpm --filter @tekon/core build

      - name: Install Playwright browser (chromium)
        run: corepack pnpm --filter @tekon/web exec playwright install --with-deps chromium

      - name: Web Playwright e2e
        run: corepack pnpm --filter @tekon/web test:e2e
