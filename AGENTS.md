# Tekon 仓库指令

Tekon 是本地 Agent workflow 框架，包含实现代码和产品、技术、验收文档。描述性文字默认用中文，代码标识、命令和引用标题可保留原文。

本文件只补充项目约定；通用工作方式和子代理调度沿用全局指令，模型与思考等级由运行时配置管理。`CLAUDE.md` 是本文件的符号链接，保持单一来源。

## 仓库导航

- `packages/core/`：workflow、provider、gate 等核心逻辑；`packages/cli/`：命令行；`packages/web/`：Web Dashboard。
- `docs/technical/`：技术方案；`docs/research/`：外部调研；新产品方案放 `docs/product/`（按需创建）。
- `docs/manual/tekon-user-manual.md`：主用户手册；同目录 `.html` 为人审版本。
- `docs/reviews/`：正式验收、发布证据、评估报告和用户要求归档的审阅记录。
- `docs/superpowers/plans/`：实施计划。

## 产品与安全边界

- 区分已实现能力和长期方向。自动推进到 PR 是产品方向；当前能力以代码和验收证据为准。Tekon 的 push、创建 PR、合入、上线及高危动作保留人工控制，不因开发自动化而绕过。
- 评估 Agent 能力时说明适用场景、失败与恢复路径、权限边界、人工兜底和测试缺口。关键技术判断应能落实到输入输出、状态边界及验证方式。
- 不提交密钥、token、生产凭证、内部高权限 URL 或其他敏感信息。

## 实现与验证

- 新增功能或修复行为前，先补充能验证需求或复现缺陷的测试，再改实现。断言覆盖受影响的正常路径、错误路径、边界条件及合法/非法状态转换；接口或输出变化时同步更新断言。避免重复测试、死路径和仅验证 mock 自身的测试。
- 新增 CLI 命令或子命令，在 `packages/cli/__tests__/e2e/` 补充真实进程测试。新增 e2e 文件统一命名为 `*.e2e.test.ts`；core/CLI 依赖该命名分离 unit 与 e2e，Web 按目录隔离 Vitest 与 Playwright。
- 开发中先运行受影响测试；提交代码、依赖或影响运行/构建/测试的配置变更前，运行 `pnpm test --run` 并全部通过，按影响范围补充 `pnpm typecheck`、`pnpm lint`、`pnpm build`。CLI e2e 涉及构建产物时，使用 `pnpm --filter @tekon/cli test:e2e` 完成构建和验证。
- 仅修改文档或代理指令时，检查差异、文件与链接、章节结构、占位内容和对应 HTML 一致性，无需运行应用测试。不为文字修改新增测试；验证结果不得宣称为运行行为已通过验收。
- 修改 `scripts/install.sh` 或 `scripts/update.sh` 后，在干净环境执行完整 smoketest，验证克隆、安装、构建和版本显示。

## 审阅与文档交付

- 行为变更、跨模块改动、安全/权限边界及正式方案或验收结论，交由独立子代理审阅；代码与测试一起审。纯措辞、排版或链接修正可自检。解决必须修复项后复查，直至无必须修复项；有依据的异议说明理由后交回复查。运行时无法提供独立审阅时，说明限制并自检。
- 产品文档讲用户、价值、场景和边界；技术文档讲架构、选型、权衡和验证；用户手册讲如何发起、得到什么、如何判断结果及当前限制。
- 外部能力和安全风险判断附资料链接，区分资料事实、对 Tekon 的推断及建议，并说明推断依据。
- 正式方案、手册和验收文档提供 HTML 人审版，修改源稿时同步更新。代理指令、README、CHANGELOG 等仓库维护文件无需另建 HTML。环境提供 `frontend-design:frontend-design` 技能时，使用它制作 HTML 人审版。
- 仅在正式验收、发布决策或用户要求归档时保存审阅记录。上述场景的证据若位于不随 git 提交的 `.tekon/`，将结论、run id、风险 Gate 结果及 eval 指标摘要归档到 `docs/reviews/`。有复用价值的失败、返工和测试缺口按需沉淀为规则、模板或评估数据。
- 用户可见行为变化时，按影响同步 `README.md`、`CHANGELOG.md`、用户手册及相关产品/技术/验收文档；开发约定变化时更新本文件。交付说明改动、验证和剩余限制，并简述相关文档未更新的理由。

## 提交与版本

- 完成验证后优先提交本次改动，不夹带已有无关修改；提交正文以中文为主。
- 根 `package.json` 的 `version` 是产品版本唯一来源。产品代码或随产品交付的文档变化，按本次最高级别升级并在提交信息中包含 `vX.Y.Z`：PATCH 用于修复、文档勘误、样式或无行为变化的清理；MINOR 用于新增功能、行为改进或用户手册新增章节；MAJOR 用于不兼容变更或正式 1.0 发布。
- 仅维护代理指令、开发流程或审阅记录且不改变产品及用户文档时，无需升级产品版本；版本级别存在实际歧义时再询问用户。
