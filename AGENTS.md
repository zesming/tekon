# Tekon 仓库指令

Tekon 是本地 Agent workflow 框架。描述性文字默认中文，代码、命令和引用标题可保留原文。通用工作方式与子代理调度沿用全局指令，模型和思考等级由运行时管理；`CLAUDE.md` 是本文件的符号链接。

## 目录与文档

- `packages/core/`：workflow、provider、gate；`packages/cli/`：命令行；`packages/web/`：Dashboard。
- [docs/README.md](docs/README.md) 是文档索引。现行事实维护在产品范围、交互设计、运行时合同和用户手册中；过程结论归并到这些固定文档，历史过程从 Git 查阅。
- `docs/reviews/` 只保存正式验收、发布证据及用户要求归档的记录；`docs/research/` 保留有独立引用价值的外部调研。
- 产品文档讲用户、价值、场景与边界；设计讲界面与交互；技术讲架构、选型、权衡和验证；手册讲操作、结果判断和限制。
- 正式方案、手册、验收文档同步提供 HTML 人审版，沿用现有样式；可用时使用 frontend-design 技能。README、CHANGELOG、代理指令无需另建 HTML。

## 授权与运行边界

- 区分代码及验收证据支持的现有能力与长期方向。Tekon 的 push、创建 PR、合入、上线及高危动作保留人工控制；开发本仓库时，在用户已授权范围内完成工作，不重复索取同一授权。
- 角色职责、artifact schema、Gate 和权限是产品合同。精简指令不可去掉这些边界；节点内执行按 artifact protocol，独立评审由外层工作流节点承担。
- 不提交密钥、token、生产凭证或内部高权限 URL。外部能力与安全判断附资料链接，区分来源事实、基于代码/证据的推断和建议。

## 实现与验证

- 新增或修复行为，先补能验证需求或复现缺陷的测试，再改实现。复用已有覆盖，检查受影响的正常/错误路径、边界和状态转换；不要重复测试、测试死路径或只验证 mock 自身。
- 新增 CLI 命令在 `packages/cli/__tests__/e2e/` 补真实进程测试，文件用 `*.e2e.test.ts`；core/CLI 按此命名分离 unit/e2e，Web 按目录分离 Vitest/Playwright。
- 开发先跑受影响测试；提交代码、依赖或运行/构建/测试配置前，`pnpm test --run` 必须全部通过，按影响补 `pnpm typecheck`、`pnpm lint`、`pnpm build`。涉及 CLI 产物时用 `pnpm --filter @tekon/cli test:e2e` 构建并验证。
- 同一测试或构建只有一个 owner 和活动执行。已有证据覆盖当前修改后，不重复全量检查，除非新增改动、失败或未覆盖风险需要；用户明确要求的全量回归仍须执行。
- 仅改文档/指令时检查差异、文件/链接、章节、占位内容和 Markdown/HTML 一致性，无需应用测试或文字断言，也不得宣称运行行为通过验收。
- 修改 `scripts/install.sh` 或 `scripts/update.sh` 后，在干净环境跑完整 smoketest，覆盖克隆、安装、构建和版本显示。

## 审阅与交付

- 行为、跨模块、安全/权限边界及正式方案/验收结论由独立子代理审阅，代码与测试一起审。必须修复项解决后复查至清零；异议给出证据再交回复查。纯措辞/排版/链接可自检；无法独立审阅时说明限制。
- 用户可见行为变化按影响更新 README、CHANGELOG、手册和相关固定文档；开发约定变化更新本文件。交付说明改动、验证、限制及相关文档未更新的理由。
- 正式验收/发布所用 `.tekon/` 证据须归档结论、run id、风险 Gate 和 eval 摘要到 `docs/reviews/`。说明适用场景、失败/恢复、权限、人工兜底与测试缺口；不要用历史或 mock 结果冒充本次真实 Provider 验收。
- 清理本次临时产物，尤其 `tmp/`；先区分用户已有文件与本次生成物，必要证据归档后再删除。

## 提交与版本

- 验证后优先提交本次改动，不夹带无关修改；正文以中文为主。
- 根 `package.json.version` 是产品版本唯一来源，子包同步。产品代码或交付文档变化按最高级别升级并在提交信息含 `vX.Y.Z`：PATCH 为修复、勘误、样式或无行为清理；MINOR 为新增功能、行为改进或手册新章节；MAJOR 为不兼容或正式 1.0。
- 仅代理指令、开发流程或审阅记录维护无需升级；版本级别有实际歧义时再问用户。
