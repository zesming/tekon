# Excerpt of `dsh --profile headless --dump-default-config` (measured 2026-08-25,
# @deepseek-ai/dsh@0.1.1-rc.2). Trimmed to the plugin ids the ACL contract
# checks for. The full dump is ~333 lines of the composed plugin tree; the
# contract only asserts substring presence of each required id (drift = a
# required id disappears), so this excerpt is a faithful fixture for L1.
plugins:
  - id: headless-runner
    name: '@deepseek-ai/dsh-headless'
  - id: sandbox-policy
    name: '@deepseek-ai/dsh-sandbox-policy'
    config:
      mode: workspace-write
      workspaceRoot: <cwd>
  - id: user-approval
    name: '@deepseek-ai/dsh-user-approval'
    config:
      policy: ask
  - id: permission
    name: '@deepseek-ai/dsh-permission-presets'
  - id: session-persistence-jsonl
    name: '@deepseek-ai/dsh-session-persistence-jsonl'
  - id: agent-default-model
    name: '@deepseek-ai/dsh-agent-default-model'
    config:
      provider: deepseek-official
      model: deepseek-v4-flash
